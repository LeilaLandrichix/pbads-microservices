spring:
  application:
    name: eureka-server
  config:
    import: optional:configserver:http://localhost:8888
  cloud:
    config:
      uri: http://localhost:8888
      name: eureka-server
      profile: default

server:
  port: 8761
